{
  "puppet_task_version": 1,
  "supports_noop": false,
  "description": "A short description of this task",
  "parameters": {
    "sourceid": {
      "description": "VMID of the vm you wish to clone",
      "type": "Integer"
    },
    "vmid": {
      "description": "VMID for the new vm",
      "type": "Integer"
    },
    "description": {
      "description": "Description for the new vm",
      "type": "Optional[String[1]]"
    },
    "storage_location": {
      "description": "storage location for new machine, default: local-lvm",
      "type": "Optional[String[1]]",
      "default": "local-lvm"
    },
    "storage_size": {
      "description": "Size is source VM storage + storage_size provided.",
      "type": "Optional[String[1]]"
    },
    "memory_size": {
      "description": "Allocated memory (MB), specify if different than source VM",
      "type": "Optional[Integer]"
    },
    "cpu_size": {
      "description": "Allocated CPU, specifiy if different than source VM",
      "type": "Optional[Integer]"
    },
    "username": {
      "description": "Name of user you would like created",
      "type": "Optional[String[1]]",
      "default": "temp"
    },
    "password": {
      "description": "Password for default user specified in username",
      "sensitive": true,
      "default": "changeme123"
    },
    "network": {
      "description": "Specify network settings for new VM",
      "type": "Optional[Hash]"
    },
    "full_clone": {
      "description": "Specify if it should be linked or full clone",
      "type": "Enum[full,linked]",
      "default": "full"
    }
  }
}
